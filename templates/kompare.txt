<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Back'n Score</title>
    <style>
        /* Configuration globale : Fond noir et plein écran sans scroll */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            background-color: black;
            overflow: hidden;
            font-family: sans-serif;
        }

        /* Conteneur de positionnement */
        .main-wrapper {
            position: relative;
            width: 100%;
            height: 100%;
        }

        /* Centrage universel de l'image (Méthode compatible Android 4+) */
        .splash-img {
            position: absolute;
            top: 0;
            bottom: 0;
            left: 0;
            right: 0;
            margin: auto;
            max-width: 100%;
            max-height: 100%;
            display: block;
        }

        /* Boîte de liens centrée au milieu de l'écran */
        .overlay-box {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            -webkit-transform: translate(-50%, -50%); /* Crucial pour le rendu Android 4 */
            background-color: white;
            padding: 30px 50px;
            border-radius: 20px;
            text-align: center;
            z-index: 10;
            box-shadow: 0 0 30px rgba(0,0,0,0.5);
        }

        /* Liens empilés verticalement */
        .link {
            display: block;
            color: black;
            text-decoration: none;
            font-weight: bold;
            font-size: 24px;
            padding: 15px 0;
            text-transform: uppercase;
        }
    </style>
</head>
<body>

    <div class="main-wrapper">
        <img src="{{ url_for('static', filename='img/splash.jpg') }}" class="splash-img">
        
        <div class="overlay-box">
            <a href="chef" class="link">Chef</a>
            <a href="pupitre" class="link">Musicien</a>
        </div>
    </div>

</body>
</html>